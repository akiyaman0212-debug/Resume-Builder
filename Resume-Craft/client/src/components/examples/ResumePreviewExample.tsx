import ResumePreview from "../ResumePreview";
import { defaultResumeData } from "@/lib/resumeTypes";

export default function ResumePreviewExample() {
  return <ResumePreview data={defaultResumeData} />;
}
